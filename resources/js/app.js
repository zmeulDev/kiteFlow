import './bootstrap';

// Alpine is included with Livewire 3 - no need to import separately
// Register custom Alpine components here using window.Alpine after Livewire initializes
document.addEventListener('alpine:init', () => {
    // Add custom Alpine components here if needed
});
